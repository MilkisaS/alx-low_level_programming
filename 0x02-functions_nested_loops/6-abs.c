#include "main.h"

/**
 * _abs - It Prints the absolute value of number.
 *
 * @num: character to be verified.
 * Return: Always positive value.
 */

int _abs(int num)
{
	if (num >= 0)
		return (num);
	else
		return (-num);
}
